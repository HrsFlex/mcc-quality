# Python cache
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/

# ── Data files — NEVER commit raw client data or live databases ─────────────────
quality.db          # SQLite live database — use data/incidents.parquet for cloud
*.db
*.db-shm
*.db-wal

# Local development artifacts
outputs/
*.log

# ── Secrets — NEVER commit credentials ─────────────────────────────────────────
.env
.streamlit/secrets.toml    # Local secrets (use Streamlit Cloud Secrets UI for cloud)

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Temporary / one-shot scripts
verify_run.py
reset_db.py
export_to_parquet.py

